---
import type { CollectionEntry } from 'astro:content'

interface Props {
  exercise: CollectionEntry<'exercises'>
}

const { exercise } = Astro.props
const { title, tags } = exercise.data
---

<a class="card" href={`/e/${exercise.id}/`}>
  <h3>{title}</h3>
  {
    tags.length > 0 && (
      <ul class="tags">
        {tags.map((tag) => (
          <li>{tag}</li>
        ))}
      </ul>
    )
  }
</a>
